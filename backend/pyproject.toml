[project]
name = "custo3d-api"
version = "0.1.0"
description = "Backend API for 3D print cost tracking"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115,<1.0",
  "uvicorn[standard]>=0.30,<1.0",
  "psycopg[binary]>=3.2,<4.0",
  "pydantic-settings>=2.5,<3.0",
]

[tool.uvicorn]
factory = false

